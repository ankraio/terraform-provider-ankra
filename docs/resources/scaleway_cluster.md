---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ankra_scaleway_cluster Resource - ankra"
subcategory: ""
description: |-
  Provisions a Scaleway-backed cluster on the Ankra platform. Provisioning arguments are immutable, so changing one forces replacement.
---

# ankra_scaleway_cluster (Resource)

Provisions a Scaleway-backed cluster on the Ankra platform. Provisioning arguments are immutable, so changing one forces replacement.

## Example Usage

```terraform
resource "ankra_scaleway_cluster" "example" {
  name                  = "scaleway-dev"
  credential_id         = "61262ddf-72db-4745-9a77-2c19961cf2d5"
  region                = "fr-par"
  zone                  = "fr-par-1"
  ssh_key_credential_id = "0f0f4d7e-2f0e-4f5a-9d6b-8c1a2b3c4d5e"

  control_plane_count = 1
  control_plane_type  = "DEV1-M"
  worker_count        = 2
  worker_type         = "DEV1-M"

  gateway_type        = "VPC-GW-S"
  gateway_allowed_ips = ["203.0.113.0/24"]

  # `retain` leaves the Scaleway resources in place when the cluster is
  # deprovisioned; `delete` releases them.
  retention_policy = "retain"
}

output "scaleway_cluster_id" {
  value = ankra_scaleway_cluster.example.cluster_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `credential_id` (String) Scaleway API credential id used to provision the cluster.
- `name` (String) Name of the cluster.
- `region` (String) Scaleway region (e.g. `fr-par`, `nl-ams`).
- `ssh_key_credential_id` (String) SSH key credential id to attach to the cluster nodes.
- `zone` (String) Scaleway zone (e.g. `fr-par-1`).

### Optional

- `bastion_port` (Number) SSH port exposed by the gateway's bastion.
- `cni` (String) Container network interface.
- `control_plane_count` (Number) Number of control-plane nodes.
- `control_plane_type` (String) Instance type for control-plane nodes.
- `description` (String) Description of the cluster.
- `distribution` (String) Kubernetes distribution: `k3s` or `kubeadm`.
- `etcd_node_count` (Number) Number of dedicated etcd nodes when `etcd_topology` is `external`.
- `etcd_topology` (String) etcd topology for kubeadm clusters: `stacked` or `external`.
- `etcd_type` (String) Instance type for dedicated etcd nodes.
- `external_cloud_provider` (Boolean) Install the cloud controller manager and CSI driver.
- `force_destroy` (Boolean) Send `force=true` when deprovisioning, which makes the platform tear down the cluster's Scaleway resources without its usual guards. Defaults to `true` for backwards compatibility; set it to `false` to take the guarded delete path. Changing this only affects a future destroy, so it never replaces the cluster.
- `gateway_allowed_ips` (List of String) CIDRs allowed to reach the public gateway.
- `gateway_type` (String) Public gateway type.
- `gitops_branch` (String) GitOps branch to commit the generated stack to.
- `gitops_credential_name` (String) GitOps GitHub credential name; commits the generated stack to Git when set with `gitops_repository`.
- `gitops_repository` (String) GitOps repository (`owner/name`) to commit the generated stack to.
- `include_dns` (Boolean) Provision a managed DNS zone for the cluster.
- `include_networking` (Boolean) Install Traefik and cert-manager for ingress.
- `kubernetes_version` (String) Kubernetes version. Defaults to the platform's stable version when unset.
- `network_ip_range` (String) Private network IP range.
- `private_network_id` (String) Existing Scaleway private network to attach to.
- `retention_policy` (String) What happens to the cluster's Scaleway resources on deprovision: `retain` or `delete`.
- `runtime_credential_id` (String) Separate credential the cluster uses at runtime, when it differs from the provisioning credential.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `wait_for_ready` (Boolean) Wait for the platform to report the cluster `running` before the resource is considered created. Defaults to `true`; set it to `false` to return as soon as the platform accepts the request. Anything that consumes the cluster - a kubeconfig, a `kubernetes` provider, a dependent module - needs the wait, because provisioning is asynchronous.
- `worker_count` (Number) Number of worker nodes.
- `worker_type` (String) Instance type for worker nodes.

### Read-Only

- `cluster_id` (String) Identifier assigned to the cluster by the Ankra platform.
- `id` (String) Identifier of the cluster (mirrors `cluster_id`).
- `kind` (String) Cluster kind the platform reports (for example `hetzner`, `imported`).
- `state` (String) Lifecycle state the platform reports for the cluster, refreshed on every read (for example `creating`, `running`, `stopped`).

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Scaleway clusters are imported by their Ankra platform cluster id.
terraform import ankra_scaleway_cluster.example ef8cf93e-1d97-4494-9de4-12ced709ffde
```
